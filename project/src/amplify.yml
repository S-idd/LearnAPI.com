version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd Assigment-no1
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: Assigment-no1/build
    files:
      - '**/*'
  cache:
    paths:
      - Assigment-no1/node_modules/**/*
